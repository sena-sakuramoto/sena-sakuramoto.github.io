# 飲食店レイアウト・シミュレーター

業態・通路幅・席ミックスなどの条件から、客席+厨房レイアウトのパターンと席数を自動生成する静的Webツール。
GitHub Pages 上では `/tools/layout-sim/` で公開される。

## 構成

| ファイル | 役割 |
|---|---|
| `presets.js` | ルールテーブル(家具寸法・業態プリセット・トイレ便器数目安)。**数値調整はここだけ触ればよい** |
| `engine.js` | 配置エンジン。ゾーニング(WC→入口→厨房→デシャップ→カウンター→通路→客席)とテーブル充填、通路幅スイープ |
| `dxf.js` | DXF(R12)書き出し。レイヤー分けされた線分+文字。Rhinoでそのまま開ける(単位mm) |
| `app.js` | UIバインド・SVG描画・KPI表示 |
| `index.html` / `style.css` | 画面 |

## 座標系

原点=店舗左前(入口側)、x=間口方向、y=奥行方向(入口 y=0、厨房が最奥)。単位mm。
DXFも同じ座標系で出力されるため、Rhino上では入口が下・厨房が上になる。

## Rhino / Grasshopper 連携

- **DXF書き出し**: そのままRhinoにインポート(レイヤー: WALL / KITCHEN / DISHUP / COUNTER / TABLE / CHAIR / AISLE / WC / ENTRANCE)
- **JSON書き出し**: 全要素の矩形データ+パラメータ+指標。GHPython側でこのJSONを読んで矩形を再構築すれば、Grasshopper上でリアルタイム編集に接続できる(Phase 2予定)

## 主な前提値(presets.js)

- 2人卓 700×700 / 4人卓 1500×750、椅子スペース450
- カウンター: 天板奥行450+椅子ゾーン600、ピッチ600〜700
- デシャップ台: 奥行600(厨房客席側)
- トイレ便器数: 客席数ベースの目安(〜20席:1、〜40席:2、〜70席:3、〜100席:4、以降50席ごと+1)。
  **実際は所轄保健所・自治体条例・ビル管理基準の確認が必須**

## 今後の拡張候補

1. GHPython版(同じルールテーブルを移植し、Rhino上でリアルタイム生成)
2. L字・不整形平面への対応(境界ポリラインの入力)
3. 壁面ベンチ(バンケット)席、小上がり・個室モジュール
4. 動線スコアリング(デシャップ〜各卓の平均距離)と複数案のパレート表示
